[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
NIXPACKS_PHP_VERSION = "8.2"

[phases.setup]
nixPackages = ["php82", "php82Extensions.mysql"]

[phases.install]
cmds = ["composer install --no-interaction --optimize-autoloader"]
